% Tikz File 'mytikz.tex'
\documentclass{standalone}
\usepackage{tikz}
%\usetikzlibrary{...}
\begin{document}
\begin{tikzpicture}
  Add grid
  \draw[gray!50, dashed, step=1] (-5.9,-0.9) grid (5.9,2.9);

  % Add axes
  % \draw[->] (0,0) -- (4,0) node[right] {\small $x$};
  \draw[-] (-5,0) -- (5,0);
  \draw[-, thick] (2,0) -- (2,2) node[right, xshift=0.3cm, yshift=0.2cm] {$\delta[n-k]$};
  \draw[-, dashed, thick] (0,0) -- (0,2);

  \foreach \x in {-4, -3, ..., 4} {
    % \pgfmathsetmacro{\x}{\i * pi}
    % \draw (\x, -0.1) -- (\x, 0.1) node [below, yshift=-0.3cm] {\small $\x$};
    % If-else statement
    \ifnum\x = 2
        % Code for the "if" condition
        % \node at (\x, 0) {This is true};
        \draw (\x, -0.1) -- (\x, 0.1) node [below, yshift=-0.3cm] {\small $k$};
        \draw[fill] (\x, 2) circle [radius=2pt];
    \else
        % Code for the "else" condition
        \draw[fill] (\x, 0) circle [radius=2pt];
    \fi
    % \draw[fill] (\x, 0) circle [radius=2pt]
    % \draw [gray, dashed] (2*pi,\y) -- (0,\y);
  }
  \node at (5.2,0.2) {n};

\end{tikzpicture}
\end{document}


